const BASE_URL = "com.vinewz.session-kit";

// OBR WINDOWS
export const ATTACH_SHEET_CONTEXT_MENU_ID = `${BASE_URL}/attach-sheet-context-menu`;
export const SHEET_POPOVER_ID = `${BASE_URL}/right-sheet-popover`;
export const DEFAULT_SHEET_POPOVER_SIZE = { width: 900, height: 900 };

// OBR BROADCAST
export const SYNC_UPDATE_CHANNEL = `${BASE_URL}/obr-sync-update-channel`;

// INDEXED DB
export const INDEXEDDB_NAME = `${BASE_URL}/db`;
